# Frontend environment variables — safe for Vite to bundle
# Never put server secrets (API keys, DB URIs, JWT secrets) here.

# URL of the backend server
VITE_BACKEND_URL=http://localhost:3000

# Currency symbol for display
VITE_CURRENCY=₹
